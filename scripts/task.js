class Task {
  constructor(id, title, importance, duration, dueDate, location, status) {
    this.id = id;
    this.title = title;
    this.importance = importance;
    this.duration = duration;
    this.dueDate = dueDate;
    this.location = location;
    this.status = status;

    this.name = "DeVonte";
  }
}
